import { useNavigate } from 'react-router-dom'
import {FC} from 'react';

const ProtectedRoutes: FC = () => {
    // const navigate = useNavigate();
    // if (localStorage) return navigate('/');
    // else return;
    return (
        <div></div>
    )
}

export default ProtectedRoutes;